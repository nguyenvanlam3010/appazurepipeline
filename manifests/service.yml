apiVersion: v1
kind: Service
metadata:
    name: nguyenvanlamappazurepipeline
spec:
    type: LoadBalancer
    ports:
    - port: 8080 
    selector:
        app: nguyenvanlamappazurepipeline